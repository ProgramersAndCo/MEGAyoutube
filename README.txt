# MEGAyoutube
This is a batch 'virus' that makes a screenshot of your IP adress and hide it.
You need to make a little .bat file and put following code to it and then run it:
@echo off
attrib -h -r -s IP.png

This unhides Screenshot of IP adress.
